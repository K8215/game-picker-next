/** @type {import('next').NextConfig} */
const nextConfig = {
	//output: "export",
	//basePath: "/projects/game-picker-next",
	eslint: {
		ignoreDuringBuilds: true,
	},
}

module.exports = nextConfig
